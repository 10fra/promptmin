# Changelog

## Unreleased

## 1.0.1 (2026-01-16)

- Add npm keywords for discoverability.

## 1.0.0 (2026-01-16)

- Add ddmin/greedy minimization with structure-aware chunking (Markdown + role blocks).
- Add preserve selectors + keep tags for non-removable regions.
- Add stability modes (`off`, `strict`, `kofn`) + `--confirm-final` verification.
- Add disk cache + artifacts (`report.md`, `meta.json`, `diff.patch`, prompt snapshots).
- Add runners: `local_command`, `openai_responses`.
- Add CI + GitHub Release workflow (tarball + checksums).
